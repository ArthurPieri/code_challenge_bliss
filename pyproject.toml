[project]
name = "code-challenge-bliss"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytz>=2024.2",
    "unidecode>=1.3.8",
]
